<!doctype html>
<html lang="en">
<head>
<meta charset="utf-8">
<meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
<title>GOALSURGE! - HALL OF FAME</title>
<link href="https://fonts.googleapis.com/css2?family=Press+Start+2P&family=VT323&display=swap" rel="stylesheet">
<style>
/* --- CORE STYLES (MATCHING INDEX) --- */
:root{
  --bg: #0f0e17;          
  --panel: #1a1a2e;       
  --neon-pink: #ff2a6d;   
  --neon-cyan: #05d9e8;   
  --neon-yellow: #ffc800; 
  --text-main: #d1d1d1;
}
*{box-sizing:border-box;margin:0;padding:0;}
body{
    margin:0; background-color: var(--bg);
    background-image: linear-gradient(rgba(255, 255, 255, 0.03) 1px, transparent 1px), linear-gradient(90deg, rgba(255, 255, 255, 0.03) 1px, transparent 1px);
    background-size: 20px 20px; color: var(--text-main); font-family:"VT323", monospace; font-size: 20px; 
    text-align:center; display:flex; flex-direction:column; align-items:center; min-height: 100vh; padding-bottom: 60px;
}

/* NAV */
.nav { width: 100%; max-width: 960px; display: flex; gap: 10px; padding: 10px; margin-top: 5px; }
.nav a { flex: 1; font-family: "Press Start 2P", cursive; font-size: 10px; padding: 12px 5px; color: var(--neon-cyan); text-decoration: none; background: #111; border: 2px solid var(--neon-cyan); box-shadow: 4px 4px 0px 0px rgba(5, 217, 232, 0.4); text-transform: uppercase; transition: all 0.1s; display: flex; align-items: center; justify-content: center; }
.nav a.active { background: var(--neon-pink); border-color: var(--neon-pink); color: #fff; }

.hero-section { width: 100%; max-width: 960px; padding: 20px 0; margin-bottom: 5px; border-bottom: 4px solid var(--neon-cyan); background: linear-gradient(180deg, rgba(5, 217, 232, 0) 0%, rgba(5, 217, 232, 0.1) 100%); position: relative; overflow: hidden; text-align: center; }
.scanlines { position: absolute; top: 0; left: 0; width: 100%; height: 100%; background: linear-gradient(to bottom, rgba(255,255,255,0), rgba(255,255,255,0) 50%, rgba(0,0,0,0.1) 50%, rgba(0,0,0,0.1)); background-size: 100% 4px; pointer-events: none; z-index: 2; }
.glitch { font-family: "Press Start 2P", cursive; font-size: 32px; color: var(--neon-yellow); text-shadow: 4px 4px 0px var(--neon-pink), -3px -3px 0px var(--neon-cyan); text-transform: uppercase; margin: 0; line-height: 1; letter-spacing: -2px; text-align: center; }
.hero-sub { font-family: "Press Start 2P", cursive; font-size: 10px; color: #fff; margin-top: 15px; letter-spacing: 3px; background: var(--neon-pink); padding: 6px 12px; display: inline-block; box-shadow: 4px 4px 0 #000; }

/* --- STICKER ALBUM STYLES --- */
.album-container {
    width: 95%; max-width: 960px;
    margin-top: 30px;
    display: grid;
    /* Responsive Grid: 2 columns on mobile, more on desktop */
    grid-template-columns: repeat(auto-fill, minmax(160px, 1fr));
    gap: 20px;
    padding: 20px;
    background: rgba(0,0,0,0.3);
    border: 4px solid #333;
}

.sticker {
    background-color: #1a1a2e;
    aspect-ratio: 3 / 4; /* Portrait sticker shape */
    position: relative;
    border: 1px dashed #555; /* Dashed line for empty slots */
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    transition: transform 0.2s;
}

/* THE "FILLED" STICKER LOOK */
.sticker.filled {
    background-color: #fff;
    border: 5px solid #fff; /* The white paper border */
    box-shadow: 5px 5px 10px rgba(0,0,0,0.8);
    transform: rotate(-1deg); /* Slight jaunty angle */
    overflow: hidden;
}

.sticker.filled:hover {
    transform: scale(1.05) rotate(0deg);
    z-index: 10;
    box-shadow: 10px 10px 15px rgba(0,0,0,0.9);
}

/* The Glossy Shine Overlay */
.sticker.filled::after {
    content: '';
    position: absolute;
    top: 0; left: 0; right: 0; bottom: 0;
    background: linear-gradient(135deg, rgba(255,255,255,0.4) 0%, rgba(255,255,255,0) 40%, rgba(255,255,255,0) 100%);
    pointer-events: none;
}

/* Sticker Label (Gold Bar at bottom) */
.sticker-label {
    position: absolute;
    bottom: 10px;
    background: var(--neon-yellow);
    color: #000;
    font-family: "Press Start 2P";
    font-size: 8px;
    padding: 5px;
    width: 90%;
    text-align: center;
    box-shadow: 2px 2px 0 #000;
    text-transform: uppercase;
    line-height: 1.2;
}

.sticker-title {
    position: absolute;
    top: 10px;
    color: #fff;
    text-shadow: 2px 2px 0 #000;
    font-family: "Press Start 2P";
    font-size: 8px;
    width: 100%;
    text-align: center;
    z-index: 2;
}

/* Empty Slot Styling */
.sticker.empty::before {
    content: '?';
    font-family: "Press Start 2P";
    font-size: 40px;
    color: #333;
}
.sticker.empty .slot-text {
    margin-top: 10px;
    font-family: "VT323";
    color: #555;
    font-size: 18px;
}

/* ============================================================
   STICKER CONFIGURATION
   YOU EDIT THIS SECTION TO ADD NEW IMAGES
   ============================================================ */

/* 1. PREMIER LEAGUE WINNER */
.sticker.premier-winner {
    /* Replace with your image URL */
    background-image: url('p1.png'); 
    background-size: cover;
    background-position: top center;
}

/* 2. WORLD CUP WINNER */
.sticker.cup-winner {
    /* Replace with your image URL */
    background-image: url('image_67be25.png'); 
    background-size: cover;
    background-position: center;
}

/* EXAMPLE FOR FUTURE STICKER */
/* .sticker.future-winner {
    background-image: url('your-new-image.png');
    background-size: cover;
} 
*/

/* Footer */
.footer-nav {
    margin-top: auto; padding-top: 40px;
    font-family: "Press Start 2P"; font-size: 10px;
    display: flex; gap: 20px;
}
.footer-nav a { color: #555; text-decoration: none; transition: color 0.3s; }
.footer-nav a:hover { color: var(--neon-cyan); }

@media (min-width: 768px) {
    .glitch { font-size: 64px; }
    .nav a { font-size: 18px; padding: 20px 10px; }
    .album-container { grid-template-columns: repeat(auto-fill, minmax(200px, 1fr)); }
    .sticker-label { font-size: 10px; }
}
</style>
</head>
<body>

<div class="nav" id="nav">
  <a href="index.html">HOME</a>
  <a href="predict.html">PLAY</a>
  <a href="scores.html">SCORES</a>
  <a href="rules.html">RULES</a>
</div>

<div class="hero-section">
    <div class="scanlines"></div>
    <h1 class="glitch">HALL OF FAME</h1>
    <div class="hero-sub">LEGENDS OF THE SURGE</div>
</div>

<div class="album-container">

    <div class="sticker filled premier-winner">
        <div class="sticker-title">PREMIER LEAGUE</div>
        <div class="sticker-label">SEASON 1 CHAMP</div>
    </div>

    <div class="sticker filled cup-winner">
        <div class="sticker-title">INT'L CUP</div>
        <div class="sticker-label">WORLD CHAMPION</div>
    </div>

    <div class="sticker empty">
        <div class="slot-text">COMING SOON</div>
    </div>
    <div class="sticker empty">
        <div class="slot-text">LOCKED</div>
    </div>
        <div class="sticker empty">
        <div class="slot-text">LOCKED</div>
    </div>
    <div class="sticker empty">
        <div class="slot-text">LOCKED</div>
    </div>

</div>

<footer class="footer-nav">
    <a href="privacy.html">PRIVACY POLICY</a>
    <a href="terms.html">TERMS OF USE</a>
</footer>

</body>
</html>
